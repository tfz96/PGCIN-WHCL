# Manuscript-aligned results

These CSV files are the manuscript-aligned outputs from MATLAB R2024a run `q2_full_20260831_nofig_recheck`. They support the structural, interface, locality, controller, and performance tables reported in the paper. They are not raw image data and contain no secret keys or credentials.

Historical smoke runs, exploratory probes, MATLAB MAT files, intermediate logs, figure-generation data, and machine-specific manifests are intentionally excluded. Re-running the suite writes fresh outputs under `experiments/runs/`.
